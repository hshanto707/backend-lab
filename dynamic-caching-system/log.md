## March

### 27 March
- Created a server that exposes an API `/api/sum` that increases the computational times each minute
- Setup Redis to cache the output & provide it from cache if available without the computation
- If the redis connection is failed, the server will work without caching.

### 28 March
- 
